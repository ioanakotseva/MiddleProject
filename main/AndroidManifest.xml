<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ioana.middleproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomeActivity" />
        <activity android:name=".RegistrationActivity" />
        <activity android:name=".ContactsActivity" />
        <activity android:name=".BagsActivity" />
        <activity android:name=".ChangePasswordActivity" />
        <activity android:name=".ManShoesActivity" />
        <activity android:name=".MyAccountActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".ShoppingCartActivity" />
        <activity android:name=".WomanShoesActivity"></activity>
    </application>

</manifest>